# RETIRED (Review K6 — 2026-07-15)
# OCI-Container-Implementierung entfernt: POL-FT-001 (Docker verboten),
# SERVER_URL-Bug (127.0.0.1 im Bridge-Netz zeigt auf Container, nicht Host),
# hartkodierte PUID=1000, docker-feishin-Unit-Referenz auf nicht-existente Unit.
# Feishin ist eine SPA -- nativer Caddy file_server waere policy-konform.
# Implementierung noch offen (kein nixpkgs-Paket verfuegbar).
{ ... }: { }
